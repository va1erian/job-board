import jobs from './jobs';
import newarticle from './postjob';
import viewjob from './viewjob';
const app = angular.module('myApp.home', [
    jobs.name,
    newarticle.name,
    viewjob.name
]);


/*
 * Route
 */
import route from './home.route';

app
    .config(route);


export default app;
