import Controller from './articles.controller.js';

export default function route($stateProvider) {
    'ngInject';

    $stateProvider
        .state('home.articles', {
            url: '/articles',
            templateUrl: 'app/home/articles/articles.html',
            controller: Controller,
            controllerAs: 'vm',
        });
}
