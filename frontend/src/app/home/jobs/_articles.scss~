.articles {
    .articles-search {
        padding-top: 20px;
        padding-bottom: 20px;

        .btn {
            height: 32px;
        }
    }

    .article {
        &:nth-child(odd) {
            background: #E5E5E5;
        }

        &:hover {
            .article-header-actions {
                display: block;
            }
        }

        .article-header {
            position: relative;

            overflow: hidden;

            &:after {
                content: '';
                clear: both;
            }
        }

        .article-header-title {
            float: left;

            font-family: $header-font;
            font-size: 25px;
            font-weight: 300;
        }

        .article-header-actions {
            float: left;
            display: none;

            margin-left: 25px;

            line-height: 32px;

            transition-duration: $default-transition-duration;

            .btn {
                padding: 5px 10px;
            }
        }

        .article-body {
            margin-top: 10px;
            margin-bottom: 10px;

            p {
                font-size: 16px;

                line-height: 26px;
            }
        }

        .article-footer {
        }

        .article-footer-tags {
            $height: 20px;
            $text-hover-color: #555;

            position: relative;
            right: $height/2;
            bottom: -($height/2);

            overflow: hidden;

            &:after {
                content: '';
                clear: both;
            }

            button {
                position: relative;
                float: left;

                height: $height;
                margin-left: 25px;
                padding: 0 5px;

                line-height: $height;

                color: #fff;
                background: $brand-primary;
                border: none;

                font-size: 12px;
                font-weight: 100;

                transition-duration: $default-transition-duration;

                &:before {
                    content: '';

                    float: left;
                    position: absolute;
                    top: 0;
                    left: -($height/2);

                    width: 0;
                    height: 0;

                    border-color: transparent $brand-primary transparent transparent;
                    border-style: solid;
                    border-width: ($height/2) ($height/2) ($height/2) 0;

                    transition-duration: $default-transition-duration;
                }

                &:hover {
                    background: $text-hover-color;

                    &:before {
                        border-color: transparent $text-hover-color transparent transparent;
                    }
                }
            }
        }
    }
}
